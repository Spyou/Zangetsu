import 'package:flutter/material.dart';

// ponytail: minimal stub so Task 4 compiles/tests; Task 5 replaces this with
// the real D-pad TV Schedule UI.
class ScheduleScreenTv extends StatelessWidget {
  const ScheduleScreenTv({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
